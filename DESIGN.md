# 🪳 蟑螂桌宠 (CockroachPet) — 设计文档

## 概述
macOS 桌宠 App，蟑螂在桌面上自由活动，用户可以互动。计划上架 App Store。

## 核心设定
- **桌宠种类**：广东蟑螂（美洲大蠊）
- **默认上限**：30 只（可在 Settings 调整，1~99）
- **可灭绝**：全部死亡后桌面清空，通过菜单栏重新召唤
- **渲染方式**：矢量绘制（SwiftUI Canvas），非 sprite

---

## 生命周期

### 启动
1. App 启动 → 菜单栏出现 🪳 图标（无 Dock 图标）
2. 恢复上次保存的蟑螂，若无则自动召唤 1 只
3. 蟑螂从屏幕底部边缘钻出（触须先探出 → 整只爬上来 → 窜到随机位置）

### 待机 (Idle) — 随机循环
- **巡逻**：随机方向慢速爬行，偶尔停下，触须摆动
- **发呆**：原地不动，触须缓慢摆动
- **沿边走**：贴着屏幕边缘走一段
- **突然窜**：随机触发，高速跑到另一位置后急停

### 关闭
- 蟑螂快速窜向最近的屏幕边缘，钻进缝里消失
- 保存状态后退出

---

## 用户交互

| 操作 | 反应 |
|------|------|
| 光标靠近 | 触须转向光标，警觉（身体微微抬起） |
| 光标快速划过 | 受惊逃窜！朝反方向高速跑开 |
| 单击 | 翻倒六脚朝天挣扎 → 几秒后翻回来逃跑 |
| 双击 | 80% 翻倒恢复 / 20% 压缩包（见下） |
| 长按拖拽 | 被捏起来 → 六条腿空中乱蹬 → 松手掉落 → 翻倒 → 爬起来跑 |
| 光标悬停不动 | 好奇靠近 → 用触须碰光标 |
| 逼到屏幕边缘 | 1~2.5 秒后展翅飞走！ |

---

## 击杀机制

### 双击
- **80% 概率**：翻倒 → 挣扎 → 翻回来跑掉
- **20% 概率**：💥 压缩包！母体死亡 → 爆出 3~5 只小蟑螂四散逃窜
  - 当前数量 ≥ 设定上限时不再触发压缩包

### ☠️ Kill All（菜单栏）
- 一键团灭所有蟑螂
- 拍扁动画 → 淡出消失

### 掉落（不致死）
- 拖拽松手 → 掉落动画 → 落地翻倒 → 爬起来跑
- 掉落判定用物理屏幕底部（不受 Dock 位置影响）
- **蟑螂不会摔死**

### 灭绝
- 全部死亡 → 桌面清空
- 通过菜单栏「Summon」重新召唤

---

## 飞行

- **触发条件**：蟑螂靠近屏幕任意边缘（150px 内）+ 光标逼近
- **等待时间**：1~2.5 秒随机
- **飞行表现**：展翅（半透明翅膀 + 振动）、身体抬起、地面阴影
- **落地**：飞到随机位置 → 收翅 → 恢复正常

---

## 小蟑螂

- 体型为成体 1/3，颜色偏浅
- 跑得比大蟑螂快
- **默认 10 分钟后长大**（可在 Settings 调整，1~60 分钟）
- 出生时间持久化，重启 App 后继续计时
- 同样可以被拍，同样有概率开压缩包

---

## 菜单栏

状态栏图标 🪳，菜单项：
- 🪳 Summon Cockroach（⌘N）— 召唤 1 只
- ☠️ Kill All（⌘K）— 一键团灭
- ⚙️ Settings（⌘,）— 打开设置
- ❌ Quit（⌘Q）— 保存并退出

---

## 设置（Settings）

| 项目 | 范围 | 默认值 |
|------|------|--------|
| 最大蟑螂数量 | 1~99 | 30 |
| 小蟑螂成长时间 | 1~60 分钟 | 10 分钟 |

设置通过 UserDefaults 持久化，实时生效。

---

## 技术方案
- **平台**：macOS 14.0+（SwiftUI + AppKit）
- **窗口**：NSWindow（透明无边框，level = .floating），每只蟑螂一个窗口
- **渲染**：SwiftUI Canvas 矢量绘制（触须、腿、翅膀、身体均为代码绘制）
- **动画**：Timer @60fps + CACurrentMediaTime delta
- **鼠标追踪**：全局 NSEvent 监听，计算光标位置和速度
- **持久化**：UserDefaults（蟑螂位置/状态/出生时间 + 用户设置）
- **App 模式**：`.accessory`（无 Dock 图标，仅菜单栏）

---

## 版本规划

### v1.0 ✅ 已实现
- [x] 蟑螂 AI 状态机（16 种状态）
- [x] 鼠标交互（靠近/逃跑/好奇/点击/拖拽）
- [x] 矢量绘制（身体/腿/触须/翅壳/眼睛）
- [x] 飞行逃跑（被逼到墙角展翅飞走）
- [x] 双击压缩包（卵荚爆出小蟑螂）
- [x] 小蟑螂成长系统
- [x] Kill All 一键团灭
- [x] Settings 页面
- [x] 持久化（重启恢复）
- [x] 菜单栏控制

### v1.1 计划
- [ ] 深夜模式（23:00 后更活跃）
- [ ] 蟑螂社交（两只相遇触须打招呼）
- [ ] 音效（爬行沙沙声、飞行嗡嗡声）
- [ ] App 图标设计
- [ ] App Store 上架（需 Apple Developer Program $99/年）
